<section class="newsletter-section">
  <div class="newsletter-bg"></div>
  <div class="section-container">
    <div class="newsletter">
      <div class="newsletter-text">
        <div class="newsletter-title">Subscribe to our newsletter</div>
        <span class="newsletter-desc">Get our latest news and offers delivered right to your inbox</span>
      </div>
      <form class="newsletter-form">
        <label for="newsletter-email" class="sr-only">Email address</label>
        <input type="email" id="newsletter-email" placeholder="Enter email address" required />
        <button type="submit" class="btn-primary newsletter-btn">Subscribe</button>
      </form>
    </div>
  </div>
</section>

<style>
  .newsletter-section {
    position: relative;
    overflow: hidden;
  }
  .newsletter-bg {
    position: absolute;
    inset: 0;
    background-image: url('/images/mink-mingle-unsplash.jpeg');
    background-size: cover;
    background-position: center;
  }
  .newsletter-bg::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.55);
  }
  .newsletter {
    position: relative;
    display: flex;
    align-items: center;
    padding: 60px 0;
  }
  @media (max-width: 1260px) {
    .newsletter {
      flex-direction: column;
      justify-content: center;
      text-align: center;
      gap: 30px;
      padding: 50px 0;
    }
  }
  @media (max-width: 610px) {
    .newsletter { padding: 40px 0; }
  }
  .newsletter-text {
    color: #ffffff;
    padding-right: 35px;
  }
  @media (max-width: 1260px) {
    .newsletter-text { padding-right: 0; }
  }
  .newsletter-title {
    font-family: "BentonSansMedium", sans-serif;
    font-size: 30px;
    line-height: 24px;
    color: #ffffff;
    margin-bottom: 15px;
  }
  @media (max-width: 768px) {
    .newsletter-title { font-size: 24px; }
  }
  @media (max-width: 450px) {
    .newsletter-title { font-size: 20px; }
  }
  .newsletter-desc {
    font-size: 18px;
    line-height: 24px;
    color: #ffffff;
  }
  @media (max-width: 768px) {
    .newsletter-desc { font-size: 16px; }
  }
  .newsletter-form {
    display: flex;
    background: #ffffff;
    box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.2);
    border-radius: 2px;
    height: 96px;
    align-items: center;
    min-width: 550px;
  }
  @media (max-width: 768px) {
    .newsletter-form { min-width: 0; width: 100%; max-width: 550px; }
  }
  @media (max-width: 610px) {
    .newsletter-form {
      height: auto;
      background: none;
      box-shadow: none;
      justify-content: center;
      flex-direction: column;
      gap: 12px;
      align-items: center;
      width: 100%;
      max-width: 400px;
      margin: 0 auto;
    }
  }
  .newsletter-form input {
    border: none;
    outline: none;
    font-family: "BentonSansMedium", sans-serif;
    font-size: 16px;
    color: #878787;
    padding: 0 25px;
    flex-grow: 1;
    height: 100%;
  }
  @media (max-width: 610px) {
    .newsletter-form input {
      display: block;
      width: 100%;
      height: 50px;
      border-radius: 4px;
      border: 1px solid rgba(255, 255, 255, 0.3);
      background: rgba(255, 255, 255, 0.1);
      color: #ffffff;
      padding: 0 16px;
      font-size: 15px;
    }
    .newsletter-form input::placeholder {
      color: rgba(255, 255, 255, 0.6);
    }
  }
  .newsletter-btn {
    width: 155px;
    min-width: 155px;
    height: 54px;
    margin: 0 17px 0 0;
    border-radius: 2px;
    box-shadow: 0px 20px 20px rgba(0, 0, 0, 0.2);
    letter-spacing: 1px;
    font-size: 14px;
  }
  @media (max-width: 610px) {
    .newsletter-btn { margin-right: 0; width: 100%; min-width: 0; }
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
</style>
